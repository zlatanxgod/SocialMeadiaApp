import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  userImageContainer: {
    borderWidth: 1,
    borderColor: '#ad1895',
    borderRadius: 50,
    padding: 3,
  },
});

export default style;
